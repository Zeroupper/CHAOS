[project]
name = "chaos"
version = "0.1.0"
description = "Collaborative Hierarchical Agents for Open Sensemaking - A multi-agent LLM system for sensemaking"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Miklos Havlik", email = "s240069@dtu.dk" }, { name = "Peter Jahola", email = "s233734@dtu.dk" }]
keywords = ["llm", "agents", "multi-agent", "sensemaking"]

dependencies = [
    "pandas>=3.0.1",
    "numpy>=2.4.2",
    "pyyaml>=6.0.0",
    "instructor>=1.14.5",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1",
    "openai>=2.24.0",
    "questionary>=2.1.1",
    "rich>=14.3.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
]
eval = [
    "sentence-transformers>=5.2.3",
    "faiss-cpu>=1.13.2",
]

[project.scripts]
chaos = "chaos.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_ignores = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=chaos"
